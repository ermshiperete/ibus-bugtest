# ibus-engine-bugtest

Minimal ibus engine to test surrounding text. This engine attempts to implement
the minimally necessary methods to get a working engine. It output the typed
characters. When typing `x` (lowercase X), the previous character gets
replaced by it's uppercase equivalent.
